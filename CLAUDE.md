# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This repository is in its earliest stage. As of now it contains only a `README.md` placeholder. No source code, build tooling, dependencies, or CI configuration exist yet.

When the project gains structure, update this file with:
- Build, lint, and test commands (including how to run a single test)
- Technology stack and key dependencies
- High-level architecture and how major components interact
- Any environment setup (`.env.example`, Docker, etc.)
